import React from "react";
import {
    View
} from "react-native";
import { TComponent } from "./constraints";
import styles from "./styles";

export const Template: React.FC<TComponent> = (props) => {
    return (
        <View style={styles.container}>
            
        </View>
    );
}