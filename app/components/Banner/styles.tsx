import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        paddingVertical: '2%',
        alignItems: 'center'
    },
    label: {
        fontWeight: '600'
    }
});